/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // All colors will come from CSS variables
        // No hardcoded values allowed
      },
      spacing: {
        // All spacing will come from CSS variables
        // No hardcoded values allowed
      },
      fontSize: {
        // All font sizes will come from CSS variables
        // No hardcoded values allowed
      },
      borderRadius: {
        // All border radius values will come from CSS variables
        // No hardcoded values allowed
      },
      boxShadow: {
        // All shadows will come from CSS variables
        // No hardcoded values allowed
      }
    },
  },
  plugins: [],
}
